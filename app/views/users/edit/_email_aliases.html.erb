<% if controller_name == 'aliases' %>
  <%= render "aliases/form", :user => current_user, :email => @email || current_user.email_aliases.new %>
<% else %>
  <%= render "aliases/list", :user => current_user, :emails => emails %>
<% end %>
