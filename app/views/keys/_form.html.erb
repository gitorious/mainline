<%= simple_form_for(ssh_key, :html => { :class => 'ssh_key' }, :url => [current_user, :keys]) do |f| -%>
  <small class="hint"><%= t("views.keys.hint").html_safe %></small>
  <%= f.input :key, :as => :text, :label => false %>
  <div class="form-actions">
    <%= button_tag t("views.common.save"), :type => 'submit', :class => 'btn btn-primary' %>
  </div>
<% end %>
