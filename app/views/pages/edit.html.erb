<h1><%= @page.new? ? t("views.common.creating") : t("views.common.editing") -%> page <%=h @page.title -%></h1>

<% form_for [@project, @page] do |f| -%>
  <%= f.text_area :content, :class => "text wide shorter" -%>
  <%= f.submit -%>
<% end -%>